@model IEnumerable<VentaAutos.Models.TFinanciamiento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Interes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Plazo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Monto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CPeriodoPago.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TVenta.IdVenta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Interes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Plazo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Monto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CPeriodoPago.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TVenta.IdVenta)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdFinanciamiento }) |
            @Html.ActionLink("Detalle", "Details", new { id=item.IdFinanciamiento }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdFinanciamiento })
        </td>
    </tr>
}

</table>
